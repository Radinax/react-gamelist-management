import React, { Fragment } from 'react'
import SearchBar from '../searchbar'

const Toolbar = () => {
  return (
    <Fragment>    
      <SearchBar />
      <button>ADD</button>
    </Fragment>
  )
}

export default Toolbar
